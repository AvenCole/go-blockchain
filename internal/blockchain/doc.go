// Package blockchain will host the core block, chain, and transaction types in
// the next implementation stage. Plan 1 keeps it as a documented domain
// boundary instead of filling it with placeholder logic.
package blockchain
